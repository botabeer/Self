# =========================
# requirements.txt للبوت LINE v3
# =========================

# إطار العمل الرئيسي للبوت
Flask==2.3.3

# للتعامل مع HTTP وطلبات API
requests==2.31.0

# لإدارة المتغيرات البيئية من .env
python-dotenv==1.0.0

# مكتبة LinePy العامة (نسخة تعمل بدون مصادقة على Render)
git+https://github.com/tenx-tech/linepy.git

# مكتبات إضافية قد يحتاجها البوت
# للتعامل مع الصور أو أي ملفات وسائط
Pillow==10.1.0

# للتعامل مع ملفات YAML (مثال: إعدادات البوت)
PyYAML==6.1.1

# مكتبات مساعدة عامة
numpy==1.27.0

# -------------------------
# ملاحظة:
# عند النشر على Render، تأكد أن Build Command هو:
# pip install -r requirements.txt
# -------------------------
